<!-- <% @components.each do |component| %>
  <h2><%= component.name %></h2>
  <iframe src="<%= preview_project_component_path(@project, component) %>"
    style="width: 100%; height: 400px; border: 1px solid #ccc; border-radius: 8px;">
  </iframe>
<% end %> -->
<div class="row">
  <div class="col-2">
    <nav id="navbar-example3" class="sticky-top pt-3 flex-column align-items-stretch pe-4 border-end">
      <% @components.each do |component| %>
        <nav class="nav nav-pills flex-column">
          <a class="nav-link" href="#component-<%= component.id %>"><%= component.name %></a>
        </nav>
      <% end %>
    </nav>
  </div>
  <div class="col-9">
    <div class="container d-flex justify-content-center mt-3">
      <h1><%= @project.title.capitalize %> Components</h1>
    </div>
    <div data-bs-spy="scroll" data-bs-target="#navbar-example3" data-bs-smooth-scroll="true" class="scrollspy-example-2" tabindex="0">
      <% @components.each do |component| %>
        <div id="component-<%= component.id %>">
          <%= link_to project_component_path(@project, component) do %>
            <h4><%= component.name %></h4>
          <% end %>
          <%= link_to "delete 🗑️", component_path(component), data: { turbo_method: :delete, turbo_confirm: "Are you sure you want to delete the component #{component.name} ?"} %>
          <iframe src="<%= preview_project_component_path(@project, component) %>"
        style="width: 100%; height: 400px; border: 1px solid #ccc; border-radius: 8px;">
          </iframe>
        </div>
      <% end %>
    </div>
  </div>
</div>
